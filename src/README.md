# ngx-lowcode demo

This directory contains the local source-linked demo app for the `ngx-lowcode` packages.

- `npm run start:demo:offline` uses local mock managers.
- `npm run start:demo:online` connects datasource and websocket managers to the BFF adapter.
- Demo routes are split into `renderer`, `designer`, and `materials` modules.
