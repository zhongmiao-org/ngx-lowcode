import { Routes } from '@angular/router';
import { DesignerDemoComponent } from './designer-demo.component';

export const routes: Routes = [
  {
    path: '',
    component: DesignerDemoComponent
  }
];
