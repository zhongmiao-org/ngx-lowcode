# Changesets

Use `npm run changeset` for code-impacting pull requests.

Rules:

- Select all changed packages.
- Include `@zhongmiao/ngx-lowcode` in every non-empty changeset.
- Choose semver bump level intentionally.
- Write a concise release note in English.
