# Changelog

## [Unreleased]

- Bump aggregate package version to `0.1.2` as release batch source.
- Finalize release archive now supports aggregate-only publish success and only writes back actually published packages.
- Initial aggregate package release for ngx-lowcode libraries.
- Bump aggregate package version to `0.1.1` as release batch source.
- Align release pipeline to sync changed subpackage versions in sandbox and write back published versions via finalize PR.
